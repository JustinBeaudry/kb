{
  "name": "cairn",
  "version": "0.6.0",
  "type": "module",
  "description": "A persistent memory plugin for Claude Code",
  "license": "MIT",
  "bin": {
    "cairn": "./src/cli.ts"
  },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "test": "bun test",
    "lint": "bunx tsc --noEmit"
  },
  "dependencies": {
    "citty": "^0.1.6",
    "yaml": "2.8.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitest": "latest"
  }
}
